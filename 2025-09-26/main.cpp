#include <iostream>

int main()
{
    //std::cout << (1 <= 2 <= 3) << '\n';

    // bool no_savings = true;
    // bool no_inheritance = false;
    // bool no_job = true;

    // bool totally_broke = (no_savings && no_inheritance && no_job);
    // std::cout << totally_broke << '\n';

    int a = 42;
    int b = 24;
    bool x = (a == b);
    std::cout << x << '\n';
    
    return 0;
}
