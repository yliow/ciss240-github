#include <iostream>
#include <cmath>

int main()
{
    // int i = 4;
    // int j = 3;
    // std::cout << i / j << '\n';
    // std::cout << double(i) / double(j) << '\n';
    // std::cout << double(i) / double(j) << '\n';
    // std::cout << (double) i / double(j) << '\n';
    // std::cout << double(i) / j << '\n';
    // std::cout << double(i / j) << '\n';

    // double x;
    // std::cin >> x;
    // double y = sqrt(x);
    // std::cout << "sqrt(x) is " << y << '\n';
    // std::cout << y * y << '\n';

    // double x, y;
    // std::cin >> x >> y;
    // std::cout << "x^y (in TI92 notation) is " << pow(x, y) << '\n';

    // const double PI = 3.14159;
    // double r;
    // std::cin >> r;
    // std::cout << "Area of circle with radius " << r << " = "
    //           << PI * r * r << '\n';
    // PI = 3;

    // char c;
    // std::cin >> c;
    // std::cout << "c = " << c << '\n';

    // char s[100];
    // std::cout << "hi what's your name: ";
    // std::cin >> s;
    // std::cout << "hi " << s << "!\n";

    std::cout << true << ' ' << false << '\n';
    return 0;
}
