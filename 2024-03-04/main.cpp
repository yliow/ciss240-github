#include <iostream>

int main()
{
    // do max yourself
    // int n; // number of values to minimize 
    // std::cin >> n;

    // int x;
    // int min;
    
    // std::cin >> x;
    // min = x;
    // std::cout << x << ' ' << min << '\n';

    // for (int i = 0; i < n - 1; ++i)
    // {
    //     std::cin >> x;
    //     if (x < min)
    //     {
    //         min = x;
    //     }
    //     std::cout << i << ' ' << x << ' ' << min << '\n';
    // }
    // std::cout << min << '\n';

    // generating equally spaced points
    // double a, b;
    // int n;
    // std::cin >> a >> b >> n;

    // double x = a;
    // double dx = (b - a) / (n - 1);
    // for (int i = 0; i < n; ++i)
    // {
    //     std::cout << x << '\n';

    //     // for next iteration
    //     x += dx;
    // }

    // another way
    // double a, b; // end points of interval
    // int n;       // number of points
    // std::cin >> a >> b >> n;
    // double dx = (b - a) / (n - 1); // length of a subinterval
    // for (double x = a; x <= b; x = x + dx)
    // {
    //     std::cout << x << '\n';
    // }
    
    return 0;
}
